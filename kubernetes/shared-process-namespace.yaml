apiVersion: v1
kind: Pod
metadata:
  name: shared-process-pod
spec:
  shareProcessNamespace: true
  containers:
  - name: app-container
    image: nginx
  - name: monitor-sidecar
    image: busybox
    command: ["/bin/sh"]
    args: ["-c", "ps aux; sleep infinity"]